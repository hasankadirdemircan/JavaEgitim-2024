package interface_examples.interface_example02;

public interface Language {
    public abstract void getName(String name);  // public and abstract keywords are redundant, because the interface is a abstract class.
}
